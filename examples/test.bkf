unsafe module exporting (Bruh, foo, aaa)

import hmm.something (a, b, c) as Bruh
import hmm.anotherThing (aa)
import hmm.something.otherThing

assume foo: Layer -> Sequence → Sequence

def Bruh = fun (a: Layer) => foo a (a Bruh.b)

